#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>
#include <set>
#include <unordered_map>
#include <bitset>
#include <unordered_map>

#define MIN(a, b) ((a) < (b) ? (a) : (b))

using namespace std;

typedef long long ll;
typedef unsigned long long ull;


int main() {
  cout << "KABIR" << endl;
}

